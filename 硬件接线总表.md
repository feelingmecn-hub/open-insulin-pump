# 闭环胰岛素泵 · 硬件接线总表（ESP32-C6）

> 权威来源：`code/esp32_firmware/src/config.h`。版本：AAPS 全兼容固件（2026-08-03 定稿）。
> ⚠️ 教学/理论验证原型，**严禁用于任何人体**；真机仅可用「空注射器+水」验证机械动作。

## 一、DRV8825 步进电机驱动（SM2012 电机）

| DRV8825 脚 | ESP32-C6 GPIO | 方向 | 有效电平 | 说明 |
|---|---|---|---|---|
| STEP | **9** | 出 | 上升沿走一步 | 硬件定时器 ISR 翻转 |
| DIR | **12** | 出 | 高=推注 / 低=退回 | （原 GPIO10，改接空 pad 12） |
| ENABLE | **13** | 出 | **低有效**（低=转/高=停） | （原 GPIO11，改接空 pad 13） |
| nFAULT | **16** | 入 | 低=故障 | 可选改接空 pad 3 恢复驱动级检测 |
| VDD | 3.3V | — | — | 逻辑电源（**务必 3.3V，非 5V**） |
| GND | GND | — | — | 逻辑地（与 VMOT 地单点共地） |

**硬件焊死（不接 ESP32）**：M0/M1/M2 = 3.3V 全高 → 固定 **1/32 微步**；nSLEEP = 3.3V 常唤醒；nRESET = 10kΩ 上拉 3.3V 不复位。

**功率侧**：VMOT → 电机电源 +；GND(VMOT) → 电源 −；电机线圈 2B/2A/1A/1B → SM2012 四线。

## 二、INA226 电流/电压监测（I2C）

| INA226 脚 | ESP32-C6 GPIO | 说明 |
|---|---|---|
| SDA | **18** | I2C 数据 |
| SCL | **19** | I2C 时钟 |

⚠️ 必须 `Wire.begin(18,19)`，不可走 Arduino 默认 I2C（被 LCD 占）。

## 三、4 键按键板（内部上拉，低有效）

| 按键 | 功能 | ESP32-C6 GPIO |
|---|---|---|
| K1 | 上 UP | **20** |
| K2 | 下 DOWN | **23** |
| K3 | 确认 SET | **4**（复用 TF_CS，不插 TF 卡） |
| K4 | 返回 ESC | **5**（复用 TF_MISO，不插 TF 卡） |

接法：每键一端接 GPIO、另一端接 GND（常开轻触开关）。

## 四、LCD 显示屏（Waveshare 1.47" SKU 28563，ST7789，172×320）

| LCD 脚 | ESP32-C6 GPIO | 说明 |
|---|---|---|
| SCK | **7** | SPI 时钟 |
| MOSI | **6** | SPI 数据 |
| CS | **14** | 片选 |
| DC | **15** | 数据/命令 |
| RST | **21** | 硬件复位 |
| BL | **22** | 背光（LEDC PWM 调光，默认 10%） |

## 五、限位开关（可选，固件已定义、板子当前未接）

| 开关 | ESP32-C6 GPIO |
|---|---|
| 前进限位 LIMIT_FWD | **2** |
| 后退限位 LIMIT_REV | **3** |

## 六、蜂鸣器（可选，固件已定义、板子当前未接）

| 功能 | ESP32-C6 GPIO |
|---|---|
| BUZZER | **0**（LEDC PWM） |

⚠️ GPIO0 是 ESP32-C6 引导脚，上电时勿拉低（不要按住接在 0 脚的设备开机）。

## 七、状态指示 / 其它

| 功能 | ESP32-C6 GPIO | 说明 |
|---|---|---|
| 状态 LED（WS2812） | **8** | 板载 |
| 5V 降压使能 EN_5V_BUCK | **17** | 可选，不用时悬空 |
| 外部看门狗 WDI | 未接（定义 -1） | 禁用 |
| 振动电机 VIBRATION | 未接（定义 -1） | 禁用 |

## 八、供电与接地要点

- **逻辑域**：ESP32-C6 + DRV8825(VDD) + INA226 共用 3.3V 与逻辑 GND。
- **功率域**：DRV8825(VMOT) + 电机线圈 共用电机电源与 VMOT GND。
- **⚠️ 逻辑 GND 与 VMOT GND 必须单点共地**（电源附近拧接回电池负极），否则丢步、推注量不准。

## 九、ESP32-C6 GPIO 占用一览

| GPIO | 用途 | 状态 |
|---|---|---|
| 0 | 蜂鸣器 | 固件定义，板未接 |
| 2 | 前进限位 | 固件定义，板未接 |
| 3 | 后退限位 | 固件定义，板未接 |
| 4 | 键 SET | 已接 |
| 5 | 键 ESC | 已接 |
| 6 | LCD MOSI | 已接 |
| 7 | LCD SCK | 已接 |
| 8 | 状态 LED | 已接 |
| 9 | 电机 STEP | 已接 |
| 10 | （原电机 DIR，已迁 12） | 释放 |
| 11 | （原电机 ENABLE，已迁 13） | 释放 |
| 12 | 电机 DIR | 已接 |
| 13 | 电机 ENABLE | 已接 |
| 14 | LCD CS | 已接 |
| 15 | LCD DC | 已接 |
| 16 | 电机 nFAULT | 定义（可选迁 3） |
| 17 | 5V 使能 | 可选 |
| 18 | INA226 SDA | 已接 |
| 19 | INA226 SCL | 已接 |
| 20 | 键 UP | 已接 |
| 21 | LCD RST | 已接 |
| 22 | LCD BL | 已接 |
| 23 | 键 DOWN | 已接 |
