# 闭环胰岛素泵外设扩展 PCB 设计指南

> 基准板：**Waveshare ESP32-C6-LCD-1.47**（自带 ESP32-C6 + 1.47" LCD + USB-C）
> 目标：在基准板的空 GPIO pad 上扩展 DRV8825（电机驱动）+ INA226（电流/电压监测）+ 4 按键 + 限位/蜂鸣器/状态 LED
> ⚠️ 本项目为**教学/理论验证原型，严禁用于任何人体**；真机仅可用「空注射器+水」验证机械动作。
> ⚠️ 以下为原型级 PCB 建议；正式产品需做 EMC/安规/失效安全冗余，本指南不含。

---

## 1. 完整网表（每条 net = 从哪连到哪）

### 1.1 ESP32-C6 ↔ DRV8825（电机控制，信号线）
| ESP32-C6 | DRV8825 脚 | 说明 |
|---|---|---|
| GPIO9 | STEP | 脉冲（硬件定时器 ISR 翻转） |
| GPIO12 | DIR | 方向，高=推注/低=退回 |
| GPIO13 | ENABLE | 低有效：低=转 / 高=停（省电+安全停） |
| GPIO16 | nFAULT | 输入，低=驱动过流/过热（可选；INA226 已覆盖监测） |
| 3.3V | VDD | **逻辑电源，务必 3.3V，不是 5V** |
| GND | GND（逻辑地） | 与 VMOT 地**单点共地** |

### 1.2 DRV8825 配置脚（不连 MCU，板内直连 3.3V 焊死）
你已确认：M0/M1/M2/nSLEEP/nRESET 全部 3.3V 常高。
- M0/M1/M2 = 3.3V → 固定 **1/32 微步**（DRV8825 全部高=1/32，TI 官方表已核实）
- nSLEEP = 3.3V → 驱动常唤醒
- nRESET = 3.3V（或经 10kΩ 上拉 3.3V）→ 不误复位
- 若用裸 DRV8825（非 Pololu 模块）：nFAULT 需外部上拉 3.3V（10kΩ）

### 1.3 DRV8825 功率侧
| 来自 | DRV8825 脚 | 说明 |
|---|---|---|
| 电机电源 +（锂电池→DC-DC） | VMOT | 电机供电，按 SM2012 额定电压 |
| 电源 − | GND（VMOT 地） | 功率地 |
| SM2012 步进电机 | 2B / 2A / 1A / 1B | 4 根线圈，顺序接（反了只反转，可现场对调） |

### 1.4 INA226（I2C 电流/电压监测）
| ESP32-C6 | INA226 脚 | 说明 |
|---|---|---|
| GPIO18 | SDA | I2C 数据 |
| GPIO19 | SCL | I2C 时钟 |
| 3.3V | VCC | 逻辑电源 |
| GND | GND | 逻辑地 |
| 电机电源 + | VBUS | 测母线电压（可选，固件目前读的是 VSHUNT 算电流） |
| 跨接 shunt 两端 | VIN+ / VIN− | 测电流；shunt 串在 VMOT+ 回路入口 |
| — | ALERT | 悬空（固件未用）；可选接 GPIO 做阈值中断 |

### 1.5 4 按键（独立 GPIO，内部上拉，低有效）
| ESP32-C6 | 按键 | 接法 |
|---|---|---|
| GPIO20 | UP | 一端 GPIO，另一端 GND |
| GPIO23 | DOWN | 一端 GPIO，另一端 GND |
| GPIO4 | SET | 一端 GPIO，另一端 GND |
| GPIO5 | ESC | 一端 GPIO，另一端 GND |




---

## 2. 布局分区（Layout）

把板子分成 4 个功能岛，岛间留电源/地分割缝：

- **A 区（中心）**：ESP32-C6 模块。所有空 GPIO pad 引到板边排针/测试点。
- **B 区（右上）**：DRV8825 + VMOT 输入端子 + 电机接口。靠近板边，远离模拟区；底层铺 VMOT 铜。
- **C 区（左下）**：INA226 + shunt 电阻。shunt 串在 VMOT+ 入口，INA226 紧挨 shunt 摆放，VIN± 走线**对称等长、短**。
- **D 区（底部边缘）**：4 按键 + 限位 + 蜂鸣器 + WS2812。按键放板边方便手按。
- **电源分割**：3.3V 逻辑平面（A/C/D 区）与 VMOT 功率平面（B 区）用分割线隔开，两平面只在**电源入口处单点连接**（星型接地）。

---

## 3. 电源设计

- **3.3V 逻辑域**：来自基准板 LDO/USB，给 ESP32 / INA226 / DRV8825 VDD / LCD。
- **VMOT 功率域**：锂电池 → DC-DC（11.1V 3S）→ VMOT，只给 DRV8825 电机线圈 + 经 shunt 给 INA226 采样。
- **去耦电容**：
  - 每个芯片 VCC-GND 旁放 100nF 陶瓷。
  - DRV8825 VMOT-GND 旁放 **100µF 电解 + 100nF 陶瓷**（低 ESR）。
  - INA226 VCC-GND 旁 100nF。
- **单点共地**：逻辑 GND 平面 与 VMOT GND **仅在电源入口拧在一起**（单点），禁止在大面积重叠——否则电机大电流回流干扰 MCU 地参考，导致丢步。
- **shunt 电阻**：低阻值功率电阻（如 0.1Ω / 2W，或按满程电流选），串在 VMOT+ 入口；INA226 VIN+ 接 shunt 的 MCU 侧、VIN− 接电源侧，走线 Kelvin 连接（4 线原则：采样线与功率线分开焊点）。

---

## 4. 走线规则

- **信号线**（STEP/DIR/ENABLE/I2C/按键）：10–15mil，尽量短。
- **STEP/DIR 关键**：避免与 INA226 模拟输入、电机线圈、VMOT 大电流线**平行长距离**；交叉时垂直穿过，防串扰导致误步。
- **I2C（SDA/SCL）**：各加 4.7kΩ 上拉到 3.3V（ESP32 内部上拉偏弱，外部更稳）；走线短，远离噪声源。
- **电源线**：3.3V ≥ 20mil；VMOT 按 SM2012 电流（通常 <1A，≥ 40mil 或铺铜）。
- **GND**：优先铺铜；功率回路（VMOT→驱动→电机→回电源）短而粗。
- **按键**：可加 100nF 对地滤波（或 RC），防抖；ESP32 内部上拉即可，无需外部电阻。
- **M0/M1/M2/nSLEEP/nRESET**：直接连 3.3V，短走线，可各加 100nF 到地防干扰。

---

## 5. 关键坑与检查清单

- ⚠️ **GPIO0 = boot 脚**：蜂鸣器若直接接 GPIO0，上电时务必别拉低（否则进下载模式）。建议蜂鸣器经 NPN 三极管低有效驱动，常态高阻/高电平，安全。
- ⚠️ **单点共地**：不共地或共地面积过大 → 地弹 → 丢步、剂量不准。这是 0.05U 精度的物理前提。
- ⚠️ **VDD 与 VMOT 绝不可接反**：VDD 必须 3.3V，VMOT 是电机电压；接反立即烧 DRV8825 和 ESP32。
- ⚠️ **nFAULT 上拉**：裸 DRV8825 需外部上拉（Pololu 兼容模块已带）；不加上拉可能误报故障。
- ⚠️ **限位开关建议接上**：作机械原点/行程硬保护，安全冗余；FWD=GPIO2 / REV=GPIO3。
- ⚠️ **丝杆/储药罐机械精度 PCB 保证不了**：靠机械件（螺距、摩擦轮直径）和 `dosing.h` 的几何参数；PCB 只保证电气稳定。
- ⚠️ **DRV8825 发热**：>1.5A/相需散热片；VMOT 走线/铺铜足以导热。

---

## 6. 推荐封装与板材（原型）

- 板材：FR-4 1.6mm，双面板足够。
- ESP32-C6：用 Waveshare 模块（排针/贴片），**不要**裸 IC（省去 RF/USB 设计）。
- DRV8825：用 Pololu 兼容模块（自带 nFAULT 上拉、电位器调电流）或裸片+散热片。
- 电机接口：2.54mm 排针 或 JST-XH。
- shunt：2512 贴片 或 插件功率电阻。
- 按键：6×6mm 贴片轻触开关。


---

## 7. 最简验证顺序（焊好板后）

1. 只焊 3.3V 域（ESP32/INA226/LCD/按键），不接 VMOT，USB 供电，确认能编译烧录、屏亮、按键响应。
2. 焊 DRV8825 控制线（9/12/13 + VDD/GND），**VMOT 暂不接电机**，用示波器看 STEP 有脉冲、ENABLE 可控。
3. 接 VMOT（空载，不接电机线圈），测 DRV8825 温升、nFAULT 电平。
4. 最后接 SM2012 线圈，用「空注射器+水」跑排气/退回，核对推出量（对比 `STEPS_PER_UNIT≈2178`）。
5. INA226 读电流/电压，比对实际。

> 任何一步异常先断电，查单点共地与 VDD/VMOT 是否接反。
